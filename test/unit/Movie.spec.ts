import { describe, it, expect } from 'vitest'

describe.skip('Movie.vue (unit) — moved to test/nuxt', () => {
  it('placeholder to avoid .vue parsing in unit project', () => {
    expect(true).toBe(true)
  })
})
